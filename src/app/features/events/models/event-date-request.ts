/**
 * Interfaz para representar la solicitud de fecha de un evento.
 * fullDate: La fecha completa del evento en formato ISO 'YYYY-MM-DD'.
 */
export interface EventDateRequest {
      fullDate: string; 
}
